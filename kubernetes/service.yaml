apiVersion: v1
kind: Service
metadata:
  name: devops-portfolio-service
  namespace: default
  labels:
    app: devops-portfolio
spec:
  type: ClusterIP
  selector:
    app: devops-portfolio
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
